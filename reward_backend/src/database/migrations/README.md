Alembic migration scripts for reward system DB.
- Run `alembic revision --autogenerate -m "initial"` to create the first migration after you set up the database.
- Use `alembic upgrade head` to apply migrations.
